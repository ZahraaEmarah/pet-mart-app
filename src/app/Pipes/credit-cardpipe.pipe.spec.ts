import { CreditCardpipePipe } from './credit-cardpipe.pipe';

describe('CreditCardpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardpipePipe();
    expect(pipe).toBeTruthy();
  });
});
